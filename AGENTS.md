# SuperRailBuilderX 開発ガイド

このファイルは、人間とCodexが同じ方針で開発するための恒久的な指示書です。
作業開始時は、このファイルと `docs/CODEX_HANDOFF.md` を最初に確認してください。

## プロジェクト概要

- KaizPatchX向けのレール制作支援ツールを開発する。
- TypeScriptとrtm-tsを使用する。
- NGTOBuilder2のツール構成と操作感をベースにする。
- 将来の他バージョン展開を考慮し、multi-target構成を維持する。

## 開発環境

- パッケージマネージャー: pnpm
- コード生成: `pnpm gen`
- ビルド: `pnpm build`
- 整形: `pnpm format`
- 整形確認: `pnpm format:check`
- multi-targetの設定: `rtmx.json`
- 共通コード: `src/common`
- ターゲット固有コード: `src/kaizpatch`、`src/mc1710`、`src/mc1122`

## 実装方針

- 入力、視線判定、プレビュー描画はクライアントスクリプトで処理する。
- RailPositionやブロックなど、ワールドを書き換える処理はサーバースクリプトで行う。
- クライアントから受け取った座標や対象は、適用前にサーバー側で再検証する。
- バージョン固有APIは直接共通コードへ書かず、compat層を経由する。
- KaizPatchX専用機能は、他ターゲットでは安全に無効化する。
- NGTOBuilder2から持ち込んだファイルは、必要に応じてSuperRailBuilderX向けに整理する。
- 既存のユーザー変更を、明確な理由なく上書きまたは削除しない。
- 不明点が結果を大きく左右する場合は、その部分の実装を止めて確認する。

## スクリプトの配置とツールキット

- `src/common/assets/minecraft/scripts/lib_hi03toolkit_1_0` 内のファイルはNGTOBuilder2と共有する参照専用ツールキットとして扱い、編集・機能追加しない。
- SuperRailBuilderXで編集・追加する共通スクリプトは、`src/common/assets/minecraft/scripts/superrailbuilderx` 内に格納する。
- ツールキットの処理を変更または拡張する必要がある場合も、`lib_hi03toolkit_1_0` は変更せず、`superrailbuilderx` 内にラッパーまたは固有実装を作成する。
- multi-targetのためにターゲット固有compatが必要な場合に限り、共通宣言を上記の共通ディレクトリへ置き、実装を各ターゲットの `assets/minecraft/scripts/superrailbuilderx` 内へ置く。

## 操作設計

- NGTOBuilder2に近い段階式操作を基本とする。
- 右クリックは選択・確定、左クリックは1段階戻る操作を優先する。
- Enterは適用、Hはヘルプ、Qはツール終了を基本キーとする。
- クリックは押下中の連続実行を避け、押された瞬間だけを検知する。

## RailPosition自由化に関する注意

- 調査結果は `docs/rail-position-free-positioning.md` を参照する。
- `RailPosition#setPosition` は精密な端点座標を変更するが、所有ブロック座標は移動しない。
- 見た目上接していても、列車のレール間移行が成立するとは限らない。
- 自動分割されたセクションレールは、通常レールと同じ方法で変更しない。

## 作業前後の情報共有

- このローカルPC環境で動作するCodexは、`docs/CODEX_HANDOFF.md` の担当名、作業記録の見出し、連絡欄の署名に「Codex」ではなく「ローカルCodex」と記載する。
- 別環境のCodexは、それと区別できる担当名を記載する。
- 開発者から実機テストやデバッグのフィードバックを受けた場合は、修正可能な箇所を実装・検証する。追加調査が必要な箇所には、クラッシュを防ぐガードと `NGTLog` などの診断出力を準備し、開発者へ確認手順とログの提出先を明示して再デバッグを依頼する。
- 開発者が格納した生ログは調査用として扱い、そのままGitへ追加しない。調査後は、対象機能の `NGTLog` 出力、関連する例外・結果、原因判断に必要な最小限の前後文脈だけを抜粋し、内容を表すファイル名へ改名して保存・コミット・pushする。PC情報、ユーザー名、ローカルパス、無関係なModログなどは抜粋から除外する。

作業開始時:

1. `docs/CODEX_HANDOFF.md` の「現在の状態」「未解決・確認待ち」「次に行うこと」を読む。
2. ローカルの変更とGitHubの最新状態を確認する。
3. 担当する項目を「作業中」に記載する。

作業終了時:

1. 実装・調査した内容を `docs/CODEX_HANDOFF.md` に追記する。
2. 実行した検証と、未実施の検証を区別して記載する。
3. 判断が必要な項目を「未解決・確認待ち」に残す。
4. 変更を伴う作業は、ユーザーから明示的に止められていない限り、検証と差分確認後にコミットする。
5. コミット後は現在のブランチをリモートへpushし、同期まで完了する。
6. コミットSHAまたはリンクと同期結果を引継ぎ帳へ記載する。pushできない場合は理由と未同期であることを残す。
7. 完了した古い「作業中」項目を整理する。

## GitHubの使い分け

- `docs/CODEX_HANDOFF.md`: 現在の作業状態や短期的な引継ぎ。
- GitHub Issue: 不具合、仕様検討、複数コミットにまたがる作業。
- コミットメッセージ: そのコミットで変更した事実。
- Pull Request: 大規模変更のレビューや、mainへ入れる前に相談したい変更。

Issueを利用した場合は、引継ぎ帳からIssueへリンクしてください。引継ぎ帳だけに長大な議論を保存しないでください。
